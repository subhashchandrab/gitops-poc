bastion:
  hosts:
    bastion:
      ansible_host: localhost
      ansible_connection: local
  vars:
    zone_compartment_id: "{{ .Values.ansibleenv.zone_compartment_id }}"
    domain: "{{ .Values.ansibleenv.domain }}"
    hostnames: 
      {{- range .Values.ansibleenv.hostnames }}
      - {{ . | quote }}
      {{- end }}